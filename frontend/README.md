# Frontend

Dieses Verzeichnis wird die HTML5-Oberfläche der Familienzentrale enthalten.

Aktueller Stand: Die erste lauffähige Version liegt noch im Repository-Root. Im nächsten Refactoring werden `index.html`, `style.css` und `app.js` hierhin verschoben und modularisiert.
